Belongs to the Week 5 exercise session (HMC versus MH). Cut from lecture note 05 when the
chapter was re-sectioned into the nine-section template: the bridge survives in the
chapter's Reading and references section as two sentences of prose, and the full table is
kept here for the session handout.

## Reading these results in the sources' notation

| This course | Source (Chib and Greenberg, Lopes) | Relation |
|---|---|---|
| kernel $\kappa(\theta \mid y)$, `log_kernel` | $\pi(\theta)$ or $f^*(\theta)$ | identical object, unnormalised |
| proposal $q(\theta' \mid \theta)$ | candidate-generating density $q(\theta, \theta')$ | identical, argument order differs |
| $\alpha(\theta, \theta')$ | probability of move $\alpha(\theta, \theta')$ | identical |
| independence proposal $q(\theta')$ | importance function $\iota(\theta)$ of Lecture 3 | same density, used as a chain rather than as weights |
| momentum $\rho$, mass matrix $M$ | $p$, and $M$ or the metric | identical |
| $\hat R$ | potential scale reduction factor, Gelman-Rubin | identical |

: Notation bridge {.striped}
